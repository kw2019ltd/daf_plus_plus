name: daf_plus_plus
description: A daf a day...
version: 0.0.1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  hive: ^1.4.0+1
  hive_flutter: ^0.3.0+1
  google_sign_in: ^4.1.4
  firebase_auth: ^0.15.4
  cloud_firestore: ^0.13.3

  fluttertoast: ^3.1.3
  google_fonts: ^0.3.9
  flutter_widgets: ^0.1.11
  flutter_sticky_header: 0.4.1
  cupertino_icons: ^0.1.2
  firebase_crashlytics: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.7.4


flutter_icons:
  android: "launcher_icon" 
  ios: true
  image_path: "assets/icon/'white-icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/lang/en.json
    - assets/lang/he.json
